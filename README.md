# SecureChatApplication
ChatApp is a simple client-server chat application built in C++, using SQLite for message and user data storage. It demonstrates socket programming, multithreading, and GUI integration for real-time messaging between multiple clients.
